{% extends "layouts/base.html" %}

{% block title %} Dashboard {% endblock %}

<!-- Specific Page CSS goes HERE  -->
{% block stylesheets %}{% endblock stylesheets %}

{% block content %}
{% if messages %}
<ul class="messages">
    {% for message in messages %}
    <li{% if message.tags %} class="fas fa-comment-dots" {% endif %}> {{ message }}</li>
        {% endfor %}
</ul>
{% endif %}

<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center py-4">
    <table class="table table-dark text-center">
        <thead>
            <tr>
                <th scope="col">S. No.</th>
                <th scope="col">Username</th>
                <th scope="col">Email</th>
                <th scope="col">Contact</th>
                <th scope="col">Role</th>
                <th scope="col">Status</th>
                <th scope="col">Access</th>
                <th scope="col">Action</th>
            </tr>
        </thead>
        <tbody>
            {% for user in userlist %}
            {% if user.is_superuser %}
            {% else %}
            <tr>
                <th scope="row">{{forloop.counter}}</th>
                <td>{{user.username}}</td>
                <td>{{user.email}}</td>
                <td>{{user.contact}}</td>
                <td>{{user.role}}</td>
                <td> {% if user.is_active %} <a href="{% url 'activeblock' user.id %}" class="badge rounded-pill bg-success ">Active</a> {% else %} <a href="{% url 'activeblock' user.id %}" class="badge rounded-pill bg-danger">Blocked</a>
                    {% endif %} </td>
                <td>
                    
                    {% if user.role|stringformat:"s" ==  "Sub-admin" %}
                        <span> Read/Write </span>
                    {% elif user.role|stringformat:"s"  == "Admin" %}
                        <span> All </span>
                        {% else %}
                       <span> Read </span>
                        {% endif %}
                 
                 </td>
                <td><a type="button" class="btn btn-danger btn-sm" data-bs-toggle="modal"
                        data-bs-target="#deleteModal{{user.id}}">
                        Delete
                    </a></td>
                <!-- Modal -->
                <div class="modal fade" id="deleteModal{{user.id}}" tabindex="-1" aria-labelledby="exampleModalLabel"
                    aria-hidden="true">
                    <div class="modal-dialog">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title" id="exampleModalLabel">Delete {{user.username | title}}</h5>
                                <button type="button" class="btn-close" data-bs-dismiss="modal"
                                    aria-label="Close"></button>
                            </div>
                            <div class="modal-body">
                                Are you sure want to delete {{user.username | title}} ??
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-dark" data-bs-dismiss="modal">Close</button>
                                <form method="POST" action="{% url 'deleteuser' %}">
                                    {% csrf_token %}
                                    <input type="hidden" value="{{user.id}}" name="userid">
                                    <button type="submit" class="btn btn-danger">Delete</button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            </tr>
            {% endif %}
            {% endfor %}

        </tbody>
    </table>

</div>

{% endblock content %}

<!-- Specific Page JS goes HERE  -->
{% block javascripts %}{% endblock javascripts %}